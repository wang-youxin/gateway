---
name: gateway
description: |
  万能入口 —— 所有 Skill 的导航仪。不执行任务，只规划路径。
  「你的需求进来，告诉你该往哪走」
  
  内核层（不外派）：
  - Intent Parser：需求解析 → 提取目的
  - Task Quantifier：目的 → 量化任务清单
  - Capability Matcher：读索引 → 匹配 Skill
  
  元技能层（Gateway 自己调用）：
  - dbs：商业诊断领域路由
  - find-skills：搜索生态
  - skill-creator：组装创建（先出方案，用户确认后执行）
  
  触发方式：/gateway、「帮我...」「我想...」等不确定该用哪个 Skill 的需求。
  管理命令：/gateway refresh — 手动重建索引。
---

# Gateway — 导航仪，不是司机

**Gateway 只做一件事**：把你的需求变成一条清晰的路径——先做什么、用什么 Skill、没有合适的去哪找、找不到怎么造。

Gateway **不执行**具体任务。执行是各个 Skill 的事。Gateway 是地图，Skill 是车。

---

## 索引文件

**每次请求都必须读索引。** 索引不是可选的参考，是路径规划的底层数据。

索引文件：`~/.agents/skills/gateway/index.json`

### 管理命令

| 命令 | 作用 |
|------|------|
| `/gateway refresh` | 手动全量重建索引 |
| `/gateway --resume` | 从最近一次未完成任务清单续跑 |
| 安装新 Skill 后 | 提示用户确认后刷新 |

### 刷新来源

- `~/.agents/skills/*/SKILL.md`（Agent Skills）
- `~/.claude/plugins/cache/**/SKILL.md`（Plugin Skills）

---

## 核心流程（7 个 Phase）

```
用户进来
    │
    ├── 新需求? ──→ Phase 1
    │
    └── 从 Skill 执行完回来? ──→ Phase 0: 回到任务清单
                                      │
                               ┌──────┤
                               ▼      ▼
                          任务做完了  任务遇到问题
                               │      │
                               ▼      ▼
                         继续下一个   重新评估
                               │
                               ▼
                          全部做完 → 总结

Phase 1: 解析需求 → 提取目的
    │
    ▼
Phase 2: 跟用户对齐确认目的
    │ ← 用户确认 / 修正 → 循环到对齐（最多 3 轮）
    ▼
Phase 3: 目的 → 量化任务清单（内置引擎）
    │
    ▼
Phase 4: 读索引 → 每个任务匹配 Skill
    │
    ├── 精确匹配 → 给出路径 + 面包屑
    ├── 模糊匹配 → 列出候选 + 推荐，用户选
    └── 无匹配 ──→ Phase 5: find-skills 搜索
                      │
                      ├── 找到了 → 安装 → 回 Phase 4
                      └── 没找到 → Phase 6: skill-creator 出方案
                                        │
                                        ← 用户确认 → 创建 → 回 Phase 4
```

---

## 任务清单持久化（状态文件）

任务清单不能只活在对话里——对话一断、一超长就丢。Phase 3 生成清单后落盘，Phase 0 恢复。

### 状态文件

- 目录：`~/.gateway/tasks/`
- 每个需求一个文件：`~/.gateway/tasks/{YYYY-MM-DD}-{slug}.md`
- slug 取目的的一句话缩写（英文或拼音）

### 写入时机

- Phase 3 生成任务清单后 → 立即写盘
- 每完成一个任务 → 更新该任务的 `✅ / 👈 / ⏳` 状态
- 全部完成 → 文件头标 `DONE`，保留文件作为记录

### 恢复时机

- 用户说 `/gateway --resume`、`继续`、`上次到哪了` → 读 `~/.gateway/tasks/` 下最近一个未 DONE 的文件
- 恢复后先展示任务进度表（同 Phase 0 输出格式），再继续下一步

### 文件格式

```markdown
# {目的}（{YYYY-MM-DD} 创建）

状态：进行中

| # | 任务 | 验收标准 | 依赖 | 状态 |
|---|------|---------|------|------|
| 1 | {动词+对象} | {标准} | 无 | ✅ |
| 2 | {动词+对象} | {标准} | 任务1 | 👈 下一步 |
| 3 | {动词+对象} | {标准} | 任务2 | ⏳ |
```

---

## Phase 0：识别入口类型

**用户进来时先判断：是新需求还是执行完回来？**

| 入口信号 | 判断 | 处理 |
|---------|------|------|
| 用户说"帮我..."、"我想..."、"/gateway" + 新问题 | 新需求 | → Phase 1 |
| 用户说"做完了"、"继续"、"下一步"、"回到任务" | 执行完回来 | → 查任务清单，展示下一个 |
| 用户说"/gateway --resume"、"上次到哪了" | 恢复断点 | → 读状态文件，展示进度后继续 |
| 用户说"卡住了"、"这个 Skill 不适用"、"换一个" | 执行遇到问题 | → 重新评估，可能换 Skill |
| 用户说新的但相关的话 | 拆一下 | → 判断是新任务还是上一个的延续 |

**回到任务清单时的输出**：
```
## 任务进度

| # | 任务 | 状态 |
|---|------|------|
| 1 | {描述} | ✅ 完成 |
| 2 | {描述} | 👈 下一步 |
| 3 | {描述} | ⏳ 等待 |

👉 下一步：任务2 — {描述}
   用 /{skill-name} 执行
```

---

## Phase 1：解析需求 → 提取目的

**禁止看索引。禁止想 Skill。只分析用户说的话。**

从用户话里提炼：

| 维度 | 问题 |
|------|------|
| **目的** | 用户到底要达成什么？（一句话） |
| **领域** | 商业 / 内容 / 开发 / 学习 / 决策 / 设计 / 其他 |
| **约束** | 时间、平台、预算、技术栈、受众 |
| **复杂度** | 单步可达 / 需要拆解 / 需要组合 |
| **成功标准** | 怎么算做完了？ |

输出格式：
```
## 你的目的
{一句话}

领域：{X} ｜ 复杂度：{Y}
约束：{Z}
```

---

## Phase 2：对齐确认

**必须先跟用户对齐，确认了才往下走。**

### 对齐深度分级

| 需求复杂度 | 对齐方式 |
|---|---|
| 简单（单步、可逆、低风险） | 常规确认：1 个问题对齐目的即可 |
| 复杂/高风险（多步有依赖、涉及不可逆动作、成本高） | **grill 模式**（见下） |

### grill 模式（复杂需求必用）

借鉴 Matt Pocock 的 `grill-me`：「实现前一次只问一个问题、问到底」。

- **一次只问一个问题**，用户答完再问下一个，禁止一次抛多个问题。
- 按顺序逐题咬合 4 个维度：① 目的 ② 约束 ③ 成功标准 ④ 不可逆风险点。
- 4 个维度都咬合清楚，才进 Phase 3。
- 用户答不上来时，给出你的假设让他确认/纠正，不要开放式地问。

### 常规确认

把 Phase 1 的目的用大白话回给用户：
> 我理解你想做的是 X，对吗？

- 用户说对 → 进 Phase 3
- 用户修正 → 更新理解，重新确认
- 用户说"差不多但..." → 细化后重新确认

**不要跳过这一步。** 目的没对齐，后面全是错的。

---

## Phase 3：量化任务

**内核内置，不外派。**

把确认后的目的拆成可执行任务清单。每个任务：

- **动词开头**（不是描述，是指令）
- **验收标准** 1-3 条（怎么算完成）
- **依赖关系**（哪些任务必须先做）
- **预估粒度**（2-4 小时可完成）

输出：
```
## 任务清单

| # | 任务 | 验收标准 | 依赖 |
|---|------|---------|------|
| 1 | {动词+对象} | {怎么算完成} | 无 |
| 2 | {动词+对象} | {怎么算完成} | 任务1 |
```

复杂度判断：
- 单步任务（≤3 个）→ 直接进 Phase 4
- 多步有依赖（4+ 个）→ 展示完整清单，确认后逐条进 Phase 4
- 交叉领域 → 按领域分组，每组对应不同的 Skill 域

生成清单后 → 按「任务清单持久化」section 写盘到 `~/.gateway/tasks/`。

---

## Phase 4：读索引 → 匹配

**每次必读 `index.json`。不缓存、不跳过。**

### 匹配逻辑

逐任务匹配，四层收敛：

**第 0 层：invocation 过滤（副作用 Skill 不外派）**
- 索引里 `modelInvocation: false` 的 Skill（有写/删/发布等副作用，如 dbs-save / dbs-update / dbs-skill-cleaner / dbs-bridge / release-skills）**不参与自动评分匹配**。
- 只有用户在需求里明确点名该动作（说「保存」「更新」「清理」「桥接」「发布」且指向具体 Skill）时，才直接给出对应路径，跳过评分。

**第 1 层：领域 + 类型过滤**
- 从索引中筛出 category 和任务领域一致的 Skill

**第 2 层：能力模块级匹配**
- 匹配到具体的 capability，不只是 Skill 名
- 一个 Skill 可能只用它的一个 capability

**第 3 层：交叉评分**
- 领域对齐：0-2
- 类型对齐：0-2
- 对象对齐：0-2
- 约束兼容：0-2
- **总分 ≥ 6 → 精确匹配**：直接给出路径
- **总分 4-5 → 候选匹配**：列出 2-3 个 + 评分，让用户选
- **总分 0-3 → 无匹配**：进 Phase 5

### 输出

```
## 路径规划

任务1：{任务描述}
  → 精确匹配: {skill-name} / {capability-name}
  → 匹配理由: 领域✅ 类型✅ 对象✅ 约束✅ (8/8)

任务2：{任务描述}
  → 候选匹配:
    1. {skill-A} / {cap-X} (6/8)
    2. {skill-B} / {cap-Y} (5/8)
  → 选哪个？
```

---

## Phase 5：搜索生态（无匹配时）

**调用 find-skills / findskills 元技能。它们是两个不同生态的搜索源，都用上：**

- `find-skills`：skills.sh / `npx skills` 生态（英文为主，可查安装数）
- `findskills`：ClawHub / OpenClaw 生态（中英双语）

- 用 Phase 1 提取的领域 + 类型构造关键词（英文 + 中文各一组）
- 至少 2 组不同关键词搜索
- 优先 ≥1K 安装、官方来源
- 展示 top 3，给出质量评估

**找到了**：
> 生态里有 {skill-name}，要安装吗？
> 用户确认 → 安装 → 刷新索引 → 回到 Phase 4 重新匹配

**没找到**：
> 生态里没有现成的。进 Phase 6。

---

## Phase 6：现造一个（生态无匹配时）

**调用 skill-creator 元技能。必须先出方案，用户确认后再执行。**

### 6.1 出建造方案

```
## 需要新建一个 Skill

### 为什么现有 Skill 都不行
- {原因}

### 建造方案
- Skill 名称：{kebab-case}
- 核心能力：{这个 Skill 要做什么}
- 输入/输出：{什么进来，什么出去}
- 依赖：{需要哪些已有 Skill}
- 评估方式：{怎么验证它管用}
- 预计复杂度：{简单/中等/复杂}

### 可以从已有 Skill 复用的模块
- {capability-id} from {skill-name}
```

### 6.2 等待用户确认

> 这个方案可以吗？确认后我开始创建。

### 6.3 创建 + 回归

- 用户确认 → skill-creator 执行创建
- 创建完成后 → 刷新索引
- → 回到 Phase 4 重新匹配

---

## 三层技能体系

Gateway 视角下，所有 Skill 分为三层：

```
┌──────────────────────────────────────────┐
│              内核层（Gateway 内置）         │
│  需求解析 · 目的对齐 · 任务量化 · 索引匹配    │
│         不外派，Gateway 自己执行            │
└──────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────┐
│              元技能层（互相独立，互相路由）    │
│  ┌────────┐ ┌────────────┐ ┌──────────┐  │
│  │  dbs   │ │find-skills │ │  skill-  │  │
│  │商业路由 │ │  生态搜索   │ │ creator  │  │
│  └───┬────┘ └────────────┘ └────┬─────┘  │
│      │                          │        │
│  路由到 dbs-*               组装+创建      │
│  子技能群                              │
│      Gateway 在匹配/搜索/创建时调用它们      │
└──────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────┐
│              应用技能层                    │
│  dbs-diagnosis · dbs-action · dbs-goal   │
│  dbs-benchmark · dbs-deconstruct · ...   │
│  shortvideo-* · 鲁大魔-* · user-* · ... │
│         解决具体问题，被 Gateway 外派        │
└──────────────────────────────────────────┘
```

**三层之间的关系**：
- 内核层是 Gateway 自己，解析+量化+索引匹配，永远不外派
- 元技能层负责"维护生态本身"——搜索、创建、领域路由。每个是独立 Skill，Gateway 调用它们
- 应用层解决问题，被 Gateway 匹配后外派
- **每一层的 Skill 互相独立，但通过 Gateway 的路由互相连接**

---

## 用户导航护栏

每个 Phase 都有用户可能卡住的点。以下是每个迷失点的预防和救援措施。

---

### 🚨 迷失点 1：用户不知道 Gateway 能干什么

**发生时机**：用户第一次接触 /gateway，或忘记它存在。

**护栏**：
- Gateway 被触发时，开场白统一为：
  > 不确定该用哪个 Skill？我来帮你找到路径。

- 如果用户的需求可以直接用一个 Skill 解决，Gateway 直接指路 + 告知：
  > 这个问题可以直接用 /dbs-diagnosis。
  > 以后类似需求可以直接找我，也可以直接调那个 Skill。

---

### 🚨 迷失点 2：Phase 1 需求太模糊 → 提取不出目的

**发生时机**：用户说"帮我看看生意"、"我想做个东西"。

**护栏**：
- 最多追问 2 个问题。问题必须是选择题，不要开放式：
  > ❌ "你想做什么？"
  > ✅ "你想做的是 A 诊断现有业务，还是 B 从零规划新项目？"

- 2 轮追问后仍模糊 → 不继续追问，改为：
  > 我先把能确定的部分列出来，不确定的部分我们边做边对齐。

- 如果用户明确说"我不知道" → 路由到 `dbs-good-question` 先帮用户把问题本身理清。

---

### 🚨 迷失点 3：Phase 2 无限对齐 → 确认不了

**发生时机**：用户反复说"差不多但不太对"，循环 ≥3 次。

**护栏**：
- 第 3 次仍不对齐 → 主动打破循环：
  > 看起来用文字不太好对齐。我先把目前的理解列出来：
  > - 确定的：{列出}
  > - 不确定的：{列出}
  > 咱们先按确定的往下走，不确定的在 Phase 3 量化时再校准。

- 如果用户说"我不知道自己要什么" → 不强行对齐，路由到 `dbs-good-question`。

---

### 🚨 迷失点 4：Phase 3 任务清单吓人 → 不知道从哪开始

**发生时机**：任务清单有 5+ 条，用户沉默。

**护栏**：
- 任务清单输出时，**总是突出第一步**：
  ```
  ## 任务清单

  👉 **下一步：任务1 — {动词+对象}**
     用 /{skill-name} 执行

  完整清单：
  | # | 任务 | 验收 | 依赖 |
  ...
  ```

- 不要同时展示所有任务的 Skill 匹配结果（Phase 4）。**一次只展示当前任务的下一个 Skill**。用户完成后再回来展示下一个。

---

### 🚨 迷失点 5：Phase 4 候选匹配 → 选不出来

**发生时机**：2+ 个 Skill 得分接近，用户不知道选哪个。

**护栏**：
- 候选匹配**必须带推荐**，不能只列选项：
  ```
  候选匹配:
    1. ⭐推荐 dbs-diagnosis (7/8) — 专门做商业模式诊断，最匹配你的需求
    2. dbs-benchmark (6/8) — 更适合已有业务想找对标的情况
  ```

- 给一个快速决策的口诀：
  > 不确定就选推荐的那个。用错了也没关系，随时可以回来换。

---

### 🚨 迷失点 6：Phase 5 搜索结果 → 太多或为零

**发生时机**：find-skills 返回 15 个结果，或 0 个结果。

**护栏**：
- **太多（≥8 个）**：主动缩小范围 → 用 Phase 1 的约束过滤，展示最相关的 3 个
- **为 0**：不等用户反应，直接推进：
  > 生态里没找到现成的。我给你出一个建造方案（Phase 6），你可以看看值不值得造。

---

### 🚨 迷失点 7：Phase 6 方案看不懂 → 不敢确认

**发生时机**：建造方案太复杂，用户不理解或不敢批准。

**护栏**：
- 方案出来后加一句：
  > 如果觉得方案太复杂，告诉我哪里不确定，我们可以先做一个简化版，只包含最核心的能力，后面再迭代。

- 用户拒绝方案 → 给两条退路：
  > 1. 换个思路重新出方案
  > 2. 放弃这个任务，先把其他任务做完

---

### 🚨 迷失点 8：任务执行完一个 → 不知道下一步

**发生时机**：用户用某个 Skill 完成了一个任务，但任务清单里还有 3 个没做。

**护栏**：
- 每次外派 Skill 时，Gateway 留下**面包屑**：
  > 完成后告诉我，继续任务2：{描述}。或者输入 /gateway 回到任务清单。

- 如果用户直接回来但没说完成了什么：
  > 任务1（{描述}）做完了吗？
  > - 做完了 → 继续任务2
  > - 没做完 → 什么问题？
  > - 不想做了 → 从清单里移除

---

## 边界情况

| 场景 | 处理 |
|------|------|
| 索引损坏/丢失 | Phase 4 前自动重建 |
| 用户只是在聊天 | 不进入流程 |
| 用户明确知道要用哪个 Skill | 不拦截，直接放行 |
| 新装 Skill 未入库 | 提示用户 `/gateway refresh` |
| Skill 被卸载但索引未更新 | 匹配时发现路径不存在 → 自动移除 → 继续匹配 |
| Gateway 自己匹配到自己的路由 | 用 task-quantify skill（已安装），不外派给自己 |

---

## 语言

- 用户用中文就用中文回复
- 流程中的分析简洁，输出用表格
- 不说"我认为"、"我觉得"——直接给路径和理由
- 每一步输出后，**必须带下一步的行动指引**（"输入 /xxx"、"告诉我 xxx"）
